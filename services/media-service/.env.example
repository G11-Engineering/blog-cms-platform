# Media Service Configuration

# Server Configuration
PORT=3003
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=media_db
DB_USER=media_user
DB_PASSWORD=media_password

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-here

# Storage Configuration
# Options: 'local', 's3', 'r2'
STORAGE_PROVIDER=local

# Local Storage Configuration (when STORAGE_PROVIDER=local)
UPLOAD_PATH=/app/uploads
MEDIA_SERVICE_URL=http://localhost:3003

# AWS S3 Configuration (when STORAGE_PROVIDER=s3)
# S3_BUCKET=my-bucket-name
# S3_REGION=us-east-1
# S3_ACCESS_KEY_ID=your-access-key-id
# S3_SECRET_ACCESS_KEY=your-secret-access-key
# S3_ENDPOINT=https://s3.amazonaws.com (optional, for S3-compatible services)

# Cloudflare R2 Configuration (when STORAGE_PROVIDER=r2)
# R2_BUCKET=my-bucket-name
# R2_ACCOUNT_ID=your-account-id
# R2_ACCESS_KEY_ID=your-access-key-id
# R2_SECRET_ACCESS_KEY=your-secret-access-key
# R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# R2_PUBLIC_URL=https://your-custom-domain.com (optional, for custom domain/CDN)

# CDN Configuration (optional, for serving files via CDN)
# CDN_URL=https://cdn.example.com

# File Upload Limits
MAX_FILE_SIZE=52428800  # 50MB in bytes
MAX_FILES_PER_UPLOAD=10

# Image Processing
THUMBNAIL_QUALITY=80
THUMBNAIL_SIZES=small:150x150,medium:300x300,large:600x600
